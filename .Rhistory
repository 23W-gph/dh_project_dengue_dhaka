install.packages("pacman")
# import, load, and prepare data-------------------------------------------------
library(readr)
library(here)
dengue_data <- read_csv(
here("dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
dengue_data <- dengue_data +
# clean column names
clean_names() +
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
) +
# remove duplicates
distinct()
dengue_data <- dengue_data +
# clean column names
clean_names() +
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
) +
# remove duplicates
distinct()
dengue_data <- dengue_data +
# clean column names
janirot::clean_names() +
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
) +
# remove duplicates
distinct()
dengue_data <- dengue_data +
# clean column names
janitor::clean_names() +
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
) +
# remove duplicates
distinct()
dengue_data <- dengue_data  %>%
# clean column names
janitor::clean_names()  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
dengue_data <- dengue_data  %>%
# clean column names
janitor::clean_names(dengue_data)  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
options(repos = c(CRAN = "https://cloud.r-project.org/"))
install.packages("pacman")
# import, load, and prepare data-------------------------------------------------
library(readr)
library(here)
dengue_data <- read_csv(
here("dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
# view data----------------------------------------------------------------------
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names
janitor::clean_names(dengue_data)  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
options(repos = c(CRAN = "https://cloud.r-project.org/"))
install.packages("pacman")
pacman::p_load(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
# import, load, and prepare data-------------------------------------------------
library(readr)
library(here)
dengue_data <- read_csv(
here("dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
# view data----------------------------------------------------------------------
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names
janitor::clean_names(dengue_data)  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
print()
print(if)
print(old_janitor)
print("old_janitor")
options(repos = c(CRAN = "https://cloud.r-project.org/"))
install.packages("pacman")
pacman::p_load(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
# import, load, and prepare data-------------------------------------------------
library(readr)
library(here)
dengue_data <- read_csv(
here("dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
# view data----------------------------------------------------------------------
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names
clean_names(dengue_data)  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
shiny::runApp()
install.packages(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
install.packages(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
install.packages(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor)
install.packages(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
install.packages(readr)
install.packages("readr")
renv::deactivate()
)
# install and load packages------------------------------------------------------
install.packages(readr, rio, here, magrittr, tidyverse, dplyr, janitor, ggplot2)
# install and load packages------------------------------------------------------
install.packages("readr", "rio", "here", "magrittr", "tidyverse", "dplyr", "janitor", "ggplot2")
# install and load packages------------------------------------------------------
install.packages("pacman")
install.packages("readr", "rio", "here", "magrittr", "tidyverse", "dplyr", "janitor", "ggplot2")
install.packages("readr", "rio", "here", "magrittr", "tidyverse", "dplyr", "janitor", "ggplot2")
?renv::status
renv::status()
# About this script-------------------------------------------------------------
# Purpose: Load, clean and prepare data
# Project task: Data visualization of a self-chosen Public Health problem
# Project title: Source reduction in IVM for dengue fever in Dhaka, Bangladesh
# Author name: Anna Wedler
# Student ID: 22306466
# Study program: MSc Global Health
# Module: Digital Health
# Examination date: February 8th 2024
# install and load packages------------------------------------------------------
install.packages("pacman")
pacman::p_load(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
# import, load, and prepare data-------------------------------------------------
library(readr)
library(here)
dengue_data <- read_csv(
here("data", "dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names
clean_names(dengue_data)  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
# About this script-------------------------------------------------------------
# Purpose: Load, clean and prepare data
# Project task: Data visualization of a self-chosen Public Health problem
# Project title: Source reduction in IVM for dengue fever in Dhaka, Bangladesh
# Author name: Anna Wedler
# Student ID: 22306466
# Study program: MSc Global Health
# Module: Digital Health
# Examination date: February 8th 2024
# install and load packages------------------------------------------------------
install.packages("pacman")
pacman::p_load(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
# import, load, and prepare data-------------------------------------------------
library(readr)
library(here)
dengue_data <- read_csv(
here("data", "dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
# view data----------------------------------------------------------------------
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names outside of mutate
clean_names()  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
# subset table with relevant data-----------------------------------------
dengue_infr <- subset(dengue_data, outcome == 1, select = c(area, outcome, area_type, house_type))
# review new data frame
print(dengue_infr)
# About this script-------------------------------------------------------------
# Purpose: Load, clean and prepare data
# Project task: Data visualization of a self-chosen Public Health problem
# Project title: Source reduction in IVM for dengue fever in Dhaka, Bangladesh
# Author name: Anna Wedler
# Student ID: 22306466
# Study program: MSc Global Health
# Module: Digital Health
# Examination date: February 8th 2024
# install and load packages------------------------------------------------------
install.packages("pacman")
pacman::p_load(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
# import, load, and prepare data-------------------------------------------------
library(readr)
library(here)
dengue_data <- read_csv(
here("data", "dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
# view data----------------------------------------------------------------------
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names outside of mutate
clean_names()  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
# subset table with relevant data-----------------------------------------
dengue_infr <- subset(dengue_data, outcome == 1, select = c(area, outcome, area_type, house_type))
# review new data frame
print(dengue_infr)
# load packages and data
pacman::p_load(
shiny,
bslib,
ggpubr,
ggplot2,
bs4Dash,
RJSONIO
)
source("clean.R")
library(readr)
library(here)
dengue_data <- read_csv(
here("dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
# view data----------------------------------------------------------------------
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names outside of mutate
clean_names()  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
# subset table with relevant data-----------------------------------------
dengue_infr <- subset(dengue_data, outcome == 1, select = c(area, outcome, area_type, house_type))
# review new data frame
print(dengue_infr)
runApp()
shiny::runApp()
runApp('scripts')
# Purpose: Create the shiny application based on the prepared data
# Project task: Data visualization of a self-chosen Public Health problem
# Project title: Source reduction in IVM for dengue fever in Dhaka, Bangladesh
# Author name: Anna Wedler
# Student ID: 22306466
# Study program: MSc Global Health
# Module: Digital Health
# Examination date: February 8th 2024
# create shiny app--------------------------------------------------------------
# load packages and data
pacman::p_load(
shiny,
bslib,
ggpubr,
ggplot2,
bs4Dash,
RJSONIO,
here
)
source("scripts/clean.R")
runApp('scripts')
runApp()
runApp()
install.packages("pacman")
pacman::p_load(readr,
rio,
here,
magrittr,
tidyverse,
dplyr,
plotly,
janitor,
ggplot2)
library(readr)
library(here)
dengue_data <- read_csv(
here("dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
# view data----------------------------------------------------------------------
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names outside of mutate
clean_names()  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
# subset table with relevant data-----------------------------------------
dengue_infr <- subset(dengue_data, outcome == 1, select = c(area, outcome, area_type, house_type))
# review new data frame
print(dengue_infr)
shiny::runApp()
# About this script-------------------------------------------------------------
# Purpose: Load, clean and prepare data
# Project task: Data visualization of a self-chosen Public Health problem
# Project title: Source reduction in IVM for dengue fever in Dhaka, Bangladesh
# Author name: Anna Wedler
# Student ID: 22306466
# Study program: MSc Global Health
# Module: Digital Health
# Examination date: February 8th 2024
# install and load packages------------------------------------------------------
install.packages("pacman")
pacman::p_load(here,
readr,
magrittr,
tidyverse,
dplyr,
janitor,
)
# import, load, and prepare data-------------------------------------------------
dengue_data <- read_csv(
here("dataset.csv"),
name_repair = "universal",
col_types = cols(
Gender = col_factor(levels = c("Female", "Male")),
Age = col_integer()),
)
# view data----------------------------------------------------------------------
View(dengue_data)
# transform data-----------------------------------------------------------------
dengue_data <- dengue_data  %>%
# clean column names outside of mutate
clean_names()  %>%
# mutate data
mutate(
across(c(area, area_type, house_type), as.factor),
outcome = as.logical(outcome)
)  %>%
# remove duplicates
distinct()
# subset table with relevant data-----------------------------------------
dengue_infr <- subset(dengue_data, outcome == 1, select = c(area, outcome, area_type, house_type))
# review new data frame
print(dengue_infr)
shiny::runApp()
shiny::runApp()
shiny::runApp()
install.packages("pacman")
pacman::p_load(git,
here,
readr,
magrittr,
tidyverse,
dplyr,
janitor,
)
install.packages(git)
install.packages(Git)
install.packages("git")
install.packages("git")
